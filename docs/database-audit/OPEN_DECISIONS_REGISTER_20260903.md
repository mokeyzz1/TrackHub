# Whole-database open-decisions register — 2026-09-03

The database audit is not complete merely because every table has a disposition. This register
keeps every semantic or operational question visible, with the evidence required before a write
can be approved. “Held” means no production mutation is authorized for that item yet.

| ID | Area | Current evidence | Decision / next gate | Status |
| --- | --- | --- | --- | --- |
| DB-01 | Person vs affiliation | `Unattached` school 1835 contains 48,124 athletes, 56,046 results, and 21 relay parents; `athlete_team_seasons` has zero linked rows. | Add explicit nullable/typed affiliation semantics in parallel; preserve placeholder until deterministic backfill and rollback pass. | Foundation applied; dual-read live; reviewed 7-row backfill applied; remaining backfill held |
| DB-02 | `Other` division | 129 schools / 245 teams / 5,654 athletes; the first 68 high-impact rows reviewed in `OTHER_DIVISION_RESEARCH_20260904.md` resolve to NCAA Division II/III, NAIA, USCAA, and Canadian U SPORTS programs. | Separate governing body from division/conference; do not reinterpret `Other`; classify only source-reviewed cohorts. | Decided: no bulk remap; evidence pass in progress |
| DB-03 | School duplicate identity | Post-cleanup scan has 2 normalized-name groups / 2 extra rows: Lewis Clark (ID) vs Lewis & Clark (OR), and West Chester (PA) vs Westchester (NY). Both are separated by state/division; the NY row is empty but name-only merging is unsafe. | Keep both remaining pairs; only merge with new source-backed evidence and a new rollback map. | Reviewed cleanup complete; intentional collisions retained |
| DB-04 | Athlete identity | Broad scan has 7,037 normalized name+school groups / 10,649 extras. Stronger source evidence finds 209 shared Athletic.net URLs (151 same-shape, 58 mixed-shape) and zero duplicate canonical TFRRS IDs in `athletes`; none of those URL keys is in `external_ids`. Relay legs separately show 96 TFRRS source IDs attached to one canonical and one nonmatching internal athlete (214 matching / 300 mismatch legs); 11 mismatch rows also disagree on display name, with mismatch rows from February–June and matching rows from August–September. | Use reviewed source IDs/aliases, gender, dates, and provenance; do not add URL uniqueness or merge/reassign from name/URL/source ID alone. Promote only reviewed keys into existing identity surfaces. | 2026-09-04 source-identity checkpoint; held |
| DB-05 | Meet identity | Production has 12,978 meets, 10 normalized duplicate TFRRS URL groups (13 extra rows), 5 normalized duplicate Athletic.net URL groups (5 extra rows), and 2 normalized name/date pairs (2 extra rows). `tfrrs_meet_id` is NULL on 12,906 rows. Official source snapshots show the first eight reused TFRRS URLs currently resolve to 2026 editions; `95531` aligns to the Mar. 26 row while `96401` aligns to Apr. 15, whose 24 private source records have 21 current links to meet 12632 and three untyped/unlinked records; meet 12562 has no linked records. AthleticLIVE confirms `66223` is a different Apr. 4 meet, `67952` spans two PCAC days, `639904` is the combined-events page, and `651507` is a near-date/name duplicate candidate. The South Coast name/date pair's only timing URL resolves to Apr. 25 conference championships while the pair is dated Apr. 21 prelims. Three historical URL pairs have byte-equivalent individual result sets (959, 1,058, and 2,487 rows); the Athletic.net pass found one 770/770 copied pair and one 194/202 overlap pair. | Define a source-aware meet key and reconcile URL aliases against source records before any uniqueness, merge, or result reassignment. Preserve both parents while fact-set lineage is reviewed. | 2026-09-04 source-ownership checkpoint; held |
| DB-06 | Result identity | `results` has 3,419,178 rows, 0 NULL `event_type_id`, 562,033 NULL `meet_id`, and 401,563 NULL `team_id`. Two valid partial unique indexes already guard linked rows; 2,283,713 legacy `event_id` values, 422,761 dates, and 313,152 meet names are NULL. The contextual unlinked duplicate scan found zero groups. | Define a source/event/round identity key and dry-run duplicate partitions after meet lineage review. Keep nullable links as semantic states; do not add another results table or force links. | 2026-09-04 result identity checkpoint; held |
| DB-07 | Relay identity | `relay_results` has 200,736 rows, 0 NULL `event_type_id`, 22,865 NULL `meet_id`, 31,344 NULL `team_id`, and no table-level unique constraint. `relay_athletes` has 3,359 NULL athlete links, 2 duplicate leg-order groups, and 459 repeated `(relay_result_id, athlete_id)` groups. The focused duplicate review finds 0 exact `(relay_result_id, athlete_id, leg_order)` duplicates: every repeat uses two different leg slots. Ninety-six TFRRS source IDs span 514 relay-leg rows across multiple parents/meets, with 214 canonical-match and 300 mismatch legs. Of 77 source-linked conflict parents, four legs had an internal-ID mismatch; two deterministic unique-target links were repaired with archived before-images and verified, while the remaining cases are held. `teams` has 3,516 school-backed rows with a `UNIQUE (school_id, gender)` key; only seven rows carry explicit affiliation labels. | Define the relay-parent key and preserve unknown/unattached relay states. Do not delete repeated legs, collapse leg slots, or reassign the held mismatch cohort until relay parents/legs map to private source records and raw observations. Do not force team links or create a generalized affiliation table until non-collegiate identity requirements are proven. | 2026-09-04 repeated-leg gate; two source-backed links repaired; remaining identity work held |
| DB-08 | Season taxonomy | Multiple live conventions (`Indoor YYYY`, `Outdoor YYYY`, `XC YYYY`, `Summer YYYY`, lowercase variants); `results.season_code` is NULL for all 3,419,178 rows. | Map deterministic meet-season values, retain raw text, hold ambiguous/NULL values; do not infer `season_code` from calendar year alone. | 2026-09-04 vocabulary checkpoint; mapping held |
| DB-09 | Environment | Outdoor/indoor/XC and 258,521 NULL result environments. | Backfill only from deterministic source/meet context; do not infer all NULLs. | 2026-09-04 vocabulary checkpoint; held |
| DB-10 | Round taxonomy | `Final` and `Finals`, `Prelim` and `Preliminaries`, numbered heats, and NULLs coexist. | Canonicalize with raw preservation; require source evidence for NULL/ambiguous rounds. | 2026-09-04 vocabulary checkpoint; held |
| DB-11 | Event model | `events` was empty, had no database dependents, and had one frontend reader that always received an empty set. | Retire after frontend reader migration, type/tests, repository search, and exact-structure rollback test. | Applied 2026-09-04; rollback tested |
| DB-12 | PR authority | `athlete_prs` has 475,523 rows and 98.2% NULL provenance fields; `v_athlete_prs` has 851,775 canonical buckets, while existing migration evidence found scraped-only career-best pairs. The points view previously folded wind/component digits into supplied scores. | Keep and reconcile the cache; migrate readers/writers, close missing-result gaps, then retire only after full-season comparison and rollback. The view now accepts only supplied leading aggregate points and excludes typed components; no source rows were changed. | View semantics fixed 2026-09-04; cache parity held |
| DB-24 | Multi-event semantics | 70,764 `results` rows classified as points carry a typed time or distance; aggregate scores and component marks share the same `event_type_id`. | Treat supplied aggregate/component values as authoritative with no recalculation; fix immediate display in the read/UI path. Model parent multi-event results and ordered components in a future schema wave only after deterministic mapping, provenance, and rollback are proven. | Evidence captured and product rule recorded 2026-09-04; schema held |
| DB-13 | Live-results lifecycle | `live_results` has 48 stale 2025 rows, all unprocessed/unfinalized and unlinked to athlete/team/meet; a compatibility view and active readers/writers remain. The view omits newer lifecycle fields and the table is broadly public-readable. No shipped frontend caller was found, but manual scraper/tooling references remain. | Live tracking is not currently in use. Preserve the table/view/permissions unchanged and defer replacement/retirement until the feature returns; then use the existing private ingest layer and an archived rollback migration. | Deferred by product priority; no row, policy, or schema change |
| DB-14 | Unmapped events | 46 raw labels have been seen 1,484 times; every label now exact-matches `event_aliases`; no unresolved labels remain. The table is service-role-only and has no canonical link/state columns. | Keep raw telemetry and use the authoritative alias join as the current resolution. Add durable review/retention state only if an operational workflow requires it; do not duplicate the canonical link or delete history. | Exact-match recheck recorded 2026-09-04; no data change |
| DB-22 | Reference identities | `external_ids` has 356 verified rows for 353 athletes; no duplicate `(source, external_key)` groups; school/team/conference links are all NULL. Three athletes have two verified source IDs each and remain review candidates. `conference_memberships` is empty while current school relationships use `schools.current_conference_id`. | Keep one shared portability map and the existing current-school relationship; do not split, bulk-backfill, or replace them. Add entity links or historical memberships only from reviewed source evidence; retain ambiguous identities separately. | 2026-09-04 reference/taxonomy checkpoint verified; no data change |
| DB-15 | Ingest queues | Meet-level `ingest.recovery_queue` has 2,573 rows with coverage states; event-level `ingest.event_recovery_queue` has 10,608 rows, required event identity, lease/retry fields, and an explicit `4x100m` scope check. Both are private service-role/postgres surfaces; two old running dry-run rows belong to the paused 4x100 scope. | Keep separate by grain; do not add a compatibility interface or change the stale paused run state until the 4x100 workstream is explicitly resumed. | 2026-09-04 private-ingest checkpoint verified; decided separate |
| DB-16 | Backup archives | All nine historical backup tables now live in the private `archive` schema; anon/authenticated have no access and `service_role` has SELECT-only recovery access. | Keep archives behind the private schema boundary; preserve exact row counts and restore evidence. | Applied 2026-09-04; ACL and counts verified |
| DB-17 | Migration history | Five directly-applied cleanup migrations were verified and repaired into the live ledger; timestamp-drifted and provenance-uncertain files remain. | Continue by explicit baseline/alignment; never replay or mark uncertain migrations, including the paused 4×100. | Partial reconciliation; affiliation foundation recorded; held |
| DB-18 | Access control | Canonical public reads are policy-backed; all ten `ingest` tables have RLS with no public policies, anon/authenticated have no schema USAGE, and service-role/postgres are the operational paths. Function review finds 17 application routines with pinned search paths, one deliberately validated security-definer RPC (`public.register_push_token`), and two application trigger helpers with implicit/public EXECUTE but no browser table-write path. A rollback-only test confirmed both triggers still fire after those grants are revoked. | Preserve the private boundary and existing public RPC contracts. Keep browser/public EXECUTE revoked for the two trigger helpers; platform-owned `storage` functions remain unchanged. Recheck grants and function boundaries after each future schema change. | 2026-09-04 ACL hardening applied and verified |
| DB-19 | Managed schemas | `auth`, `storage`, `realtime`, `vault`, GraphQL, and extensions are platform-owned. | Leave untouched; document findings as platform configuration issues. | Decided |
| DB-20 | Performance | Large facts have many indexes but usefulness has not been workload-validated. | Use query plans/observed workload before dropping or adding indexes. | Held |
| DB-21 | Historical affiliation | 7,357 bridge rows (5,655 athletes) link a historical team to a different current athlete school. | Treat `athlete_team_seasons` as historical evidence; never overwrite it during current-school cleanup. | Evidence captured; held |
| DB-23 | Private evidence retention | Observations (156,385), quarantine (9,648), source links (41,214), source records (54,518), runs (1,497), and cleanup archive (140,207) are active provenance/control-plane data. 13,304 source records remain unlinked; no orphan links or observation source/run references exist. | Preserve append-only history; do not purge unlinked evidence, open quarantine, or before-images. Define retention only after source-link and rollback requirements are closed. | 2026-09-04 private-ingest checkpoint verified; held |

## Completion rule

An audit wave is complete only when each applicable row is either **decided with evidence** or has a
review owner and a reproducible gate. No schema/data write should be generated for a row marked
Held. This register is intentionally additive: it does not create a database table or change live
data.
